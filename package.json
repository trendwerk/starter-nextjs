{
  "name": "nextjs-starter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "format": "prettier --write '**/*.{js,json}'",
    "prepare": "if [ ! -f .env.local ]; then cp .env.local.example .env.local; fi",
    "build": "next build",
    "start": "next"
  },
  "dependencies": {
    "@tailwindcss/ui": "^0.3.1",
    "next": "9.4.4",
    "postcss-flexbugs-fixes": "^4.2.1",
    "postcss-preset-env": "^6.7.0",
    "react": "16.13.1",
    "react-dom": "16.13.1",
    "tailwindcss": "^1.4.6"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
